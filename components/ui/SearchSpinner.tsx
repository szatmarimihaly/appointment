"use client";

import CircularProgress from '@mui/material/CircularProgress';

const SearchSpinner = () => {
  return <CircularProgress size={40} sx={{ color : "white" }}/>
}

export default SearchSpinner